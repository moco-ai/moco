# moco for VS Code

moco agent integration for VS Code.

## Features
- **moco Run**: Execute tasks via command palette.
- **Task Run on File**: Process files via context menu.
- **Status Bar**: Always see the current profile.
- **Settings**: Configuration for provider and profile.

## Installation
1. Install moco CLI (`pip install moco-agent`)
2. Build and install this extension.
